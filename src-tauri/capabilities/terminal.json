{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "terminal",
  "description": "Capability for terminal windows",
  "windows": ["terminal-*"],
  "permissions": [
    "core:event:default",
    "core:window:allow-set-title",
    "log:default"
  ]
}

